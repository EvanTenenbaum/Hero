# Hero IDE Web

Hero IDE is now a mobile-first web application optimized for iOS Safari and macOS browsers.

## Archived Expo Codebase

The previous Expo/React Native implementation has been archived to:

```
archive/legacy-expo/
```

## Development

```bash
pnpm install
pnpm dev
```
